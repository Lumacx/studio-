// Original content:
// declare module 'firebase/auth';

// This declaration is likely interfering with the actual Firebase SDK types.
// Modern Firebase SDKs (v9+) provide their own comprehensive type definitions.
// It's recommended to remove or comment it out unless you have a specific
// reason to augment the Firebase module with additional types here.

// Removing it should resolve "Cannot use namespace 'User' as a type" errors.
